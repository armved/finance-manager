PORT=3001
HOST=0.0.0.0
LOG_LEVEL=info
NODE_ENV=development

DATABASE_URL=postgresql://finance:finance@localhost:5432/finance_manager

# CORS — set to your frontend URL in production (e.g. https://finance.example.com)
# If unset in production, all cross-origin requests are rejected.
# CORS_ORIGIN=http://localhost:5173

# AI receipt parsing (v2+)
# AI_PROVIDER=mock
